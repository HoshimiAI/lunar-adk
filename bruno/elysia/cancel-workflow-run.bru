meta {
  name: Cancel workflow run
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/workflow-runs/{{workflowRunId}}/cancel
  body: none
  auth: none
}

headers {
  cookie: {{sessionCookie}}
}

assert {
  res.status: eq 200
}
