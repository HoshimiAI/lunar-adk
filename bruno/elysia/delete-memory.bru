meta {
  name: Delete curated memory
  type: http
  seq: 22
}

delete {
  url: {{baseUrl}}/memories/{{memoryId}}
  body: none
  auth: none
}

headers {
  cookie: {{sessionCookie}}
}

assert {
  res.status: eq 200
  res.body.deleted: eq true
}
