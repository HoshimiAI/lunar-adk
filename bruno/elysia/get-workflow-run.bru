meta {
  name: Get workflow run by id
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/workflow-runs/{{workflowRunId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: isString
  res.body.workflow: isString
}
